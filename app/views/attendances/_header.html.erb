<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to image_tag("logo.png", class:"logo"), '/' %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <% unless controller_name == 'attendances' && action_name == 'index' %>
          <li class="nav-item">
            <%= link_to attendances_path, class: "nav-link" do %>
              <i class="fas fa-calendar-alt"></i> 日付選択
            <% end %>
          </li>
        <% end %>
        <% unless controller_name == 'workers' && action_name == 'new' %>
          <li class="nav-item">
            <%= link_to new_worker_path, class: "nav-link" do %>
              <i class="fas fa-user-friends"></i> 作業員追加
            <% end %>
          </li>
        <% end %>
        <% unless controller_name == 'vehicles' && action_name == 'new' %>
          <li class="nav-item">
            <%= link_to new_vehicle_path, class: "nav-link" do %>
              <i class="fas fa-truck"></i> 車両追加
            <% end %>
          </li>
        <% end %>
        <li class="nav-item">
          <%= link_to materials_path, class: "nav-link" do %>
            <i class="fas fa-gem"></i> 資産管理
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to destroy_user_session_path, method: :delete, class: "nav-link" do %>
            <i class="fas fa-sign-out-alt"></i> ログアウト
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
