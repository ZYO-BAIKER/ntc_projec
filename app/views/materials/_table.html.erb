<table class="full-width" >
    <tr>
      <th>資産名</th>
      <th>メーカー</th>
      <th>会社</th>
      <th>現場①</th>
      <th>現場②</th>
      <th>現場③</th>
      <th>修理中</th>
    </tr>
    <% @materials.each do |material| %>
      <tr onclick= window.location="<%= "/materials/" + material.id.to_s + "/edit" %>"  role="link" class="record">
        <td>
          <%= material.material_name %>  <%# 資産名 %>
        </td>

        <td>
          <%= material.maker %>  <%# メーカー %>
        </td>

        <% if material.place == "現場" && material.other_places != nil %>
          <td>
            <%= material.other_places %>  <%# 場所(現場) %>
          </td>
        <% elsif material.place == "修理中"%>
          <td>
            <%= "修理中" %>  <%# 場所(修理中) %>
          </td>
        <% else %>
          <td>
            <%= material.place %>  <%# 場所 %>
          </td>
        <% end %>

        <% if material.user == "その他" && material.other_users != nil %>
          <td>
            <%= material.other_users %>  <%# その他使用者 %>
          </td>
        <% else %>
          <td>
            <%= material.user %>  <%# 使用者 %>
          </td>
        <% end %>

        <td>
          <%= material&.period_start %> ~ <%= material&.period_end %>  <%# 使用期間 %>
        </td>

        <td>
          <%= material.inspection_date %>  <%# 修理点検日 %>
        </td>
      </tr>
    <% end %>
</table>
