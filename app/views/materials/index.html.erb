<%= render "header" %>

<div class="container-fluid my-3">
  <%= render "search" %>
  <div>
    全資産：<%= Material.all.count %>
    修理中：<%= Repair.where.not(repair_request: nil, repair_count: nil).count %>
  </div>
  <%= paginate @materials %>
  <div class="table-responsive">
    <%= render "table" %>
  </div>
</div>
